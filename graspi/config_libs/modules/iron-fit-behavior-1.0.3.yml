webcomponent: true

dependencies:
  - polymer-1.1.0

tasks:
  graspi_build:
    - graspi_templates_copy
    - graspi_templates_concat
    - graspi_replace

options:
  basePath: 'bower_components/iron-fit-behavior'
  destFolder: 'iron-fit-behavior/webcomponents'
  templates:
    destFile: 'iron-fit-behavior.html'
    files:
      - iron-fit-behavior.html
  replace:
    - files:
        - '%%destPath%%/iron-fit-behavior.html'
      pattern:
        pattern: '(?:\<link)(?:.*)(?:\>)'
        modifiers: 'g'
        quotesPlaceholder: 'QUOTES1'
        doubleQuotesPlaceholder: 'QUOTES2'
      replace: ''
