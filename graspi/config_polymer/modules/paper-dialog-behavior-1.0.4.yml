webcomponent: true

dependencies:
  - polymer-1.1.0
  - iron-overlay-behavior-1.0.5
  - paper-styles-1.0.11

tasks:
  graspi_build:
    - graspi_templates_copy
    - graspi_templates_concat
    - graspi_replace

options:
  basePath: 'bower_components/paper-dialog-behavior'
  destFolder: 'paper-dialog-behavior/webcomponents'
  templates:
    destFile: 'paper-dialog-behavior.html'
    files:
      - paper-dialog-behavior.html
  replace:
    - files:
        - '%%destPath%%/paper-dialog-behavior.html'
      pattern:
        pattern: '(?:\<link)(?:.*)(?:\>)'
        modifiers: 'g'
        quotesPlaceholder: 'QUOTES1'
        doubleQuotesPlaceholder: 'QUOTES2'
      replace: ''
