webcomponent: true

dependencies:
  - polymer-1.1.0

tasks:
  graspi_build:
    - graspi_templates_copy
    - graspi_templates_concat
    - graspi_replace

options:
  basePath: 'bower_components/iron-range-behavior'
  destFolder: 'iron-range-behavior/webcomponents'
  templates:
    destFile: 'iron-range-behavior.html'
    files:
      - iron-range-behavior.html
  replace:
    - files:
        - '%%destPath%%/iron-range-behavior.html'
      pattern:
        pattern: '(?:\<link)(?:.*)(?:\>)'
        modifiers: 'g'
        quotesPlaceholder: 'QUOTES1'
        doubleQuotesPlaceholder: 'QUOTES2'
      replace: ''
