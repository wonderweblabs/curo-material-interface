webcomponent: true

dependencies:
  - polymer-1.1.0

tasks:
  graspi_build:
    - graspi_templates_copy
    - graspi_templates_concat
    - graspi_replace

options:
  basePath: 'bower_components/iron-resizable-behavior'
  destFolder: 'iron-resizable-behavior/webcomponents'
  templates:
    destFile: 'iron-resizable-behavior.html'
    files:
      - iron-resizable-behavior.html
  replace:
    - files:
        - '%%destPath%%/iron-resizable-behavior.html'
      pattern:
        pattern: '(?:\<link)(?:.*)(?:\>)'
        modifiers: 'g'
        quotesPlaceholder: 'QUOTES1'
        doubleQuotesPlaceholder: 'QUOTES2'
      replace: ''
