webcomponent: true

dependencies:
  - polymer-1.1.0

tasks:
  graspi_build:
    - graspi_templates_copy
    - graspi_templates_concat
    - graspi_replace

options:
  basePath: 'bower_components/iron-a11y-keys-behavior'
  destFolder: 'iron-a11y-keys-behavior/webcomponents'
  templates:
    destFile: 'iron-a11y-keys-behavior.html'
    files:
      - iron-a11y-keys-behavior.html
  replace:
    - files:
        - '%%destPath%%/**/*.html'
      pattern:
        pattern: '(?:\<link)(?:.*)(?:\>)'
        modifiers: 'g'
        quotesPlaceholder: 'QUOTES1'
        doubleQuotesPlaceholder: 'QUOTES2'
      replace: ''
