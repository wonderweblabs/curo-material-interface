webcomponent: true

dependencies:
  - polymer-1.0.8

tasks:
  graspi_build:
    - graspi_templates_copy
    - graspi_templates_concat
    - graspi_replace

options:
  basePath: 'bower_components/iron-a11y-keys-behavior'
  destPath: 'graspi_build/iron-a11y-keys-behavior/webcomponents'
  templates:
    destFile: 'iron-a11y-keys-behavior.html'
    files:
      - iron-a11y-keys-behavior.html
  replace:
    - files:
        - 'graspi_build/iron-a11y-keys-behavior/**/*.html'
      pattern:
        pattern: '(?:\<link)(?:.*)(?:\>)'
        modifiers: 'g'
        quotesPlaceholder: 'QUOTES1'
        doubleQuotesPlaceholder: 'QUOTES2'
      replace: ''
